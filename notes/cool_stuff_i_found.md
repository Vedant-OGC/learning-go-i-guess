# Cool stuff i found in Go

- **Goroutines:** concurrent programming without heavy OS threads. Just add `go` and it runs concurrently.
- **Interfaces:** implicitly implemented, decoupled design.
- **Multiple return values:** returning a value and an error together. It is very neat.
