# learning-go-i-guess
ok so im learning go.

## progress so far
- basics (variables, types, constants, scanner)
- control flow (loops, ifs, switch)
- functions (closures, recursion)
- data structures (slices vs arrays, maps, structs)
- pointers (wtf is & and *, but actually useful)
- interfaces (polymorphism, stringer)
- error handling (if err != nil, panic, recover)
- concurrency (goroutines go brrr, waitgroup, channels, select, mutex)

goroutines are actually insane. you just add 'go' and it runs concurrently?? wild.
